<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.nuzzel.jay.simpletodo.EditItemActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/edit_task_prompt"
        android:id="@+id/editPrompt"
        android:padding="@dimen/default_padding"
        android:gravity="center"
        android:layout_above="@+id/revisedTask"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/revisedTask"
        android:hint="@string/old_task_text"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/save"
        android:id="@+id/save"
        android:onClick="onSave"
        android:layout_below="@+id/revisedTask"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="@dimen/default_img_size"
        android:layout_height="@dimen/default_img_size"
        android:id="@+id/imageView"
        android:src="@drawable/ic_launcher"
        android:padding="@dimen/default_padding"
        android:layout_above="@+id/editPrompt"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
